query GetCharacters($page: Int) {
  characters(page: $page, filter: { name: "rick" }) {
    info {
      count
      pages
    }
    results {
      name
      image
      origin {
        dimension
      }
    }
  }
}
